import { LoginForm } from "@/components/auth/LoginForm";

export const metadata = {
    title: "Login - OpenGrid",
    description: "Sign in to OpenGrid Demo Electrical Monitoring System",
};

export default function LoginPage() {
    return <LoginForm />;
}
